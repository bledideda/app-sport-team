import { View, Text } from "react-native";

export default function TeamScreen(props)
{  
    return (
        <View {...props}>
            <Text>This is the TeamScree of the app</Text>
        </View>
    )
}